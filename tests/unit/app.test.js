/**
 * Legacy top-level app bootstrap suite placeholder.
 * The historical app entrypoint at src/app/app.js was removed,
 * so these integration-style tests are skipped until a replacement exists.
 */
describe.skip('app.js bootstrap (legacy)', () => {
  test('skipped: app entry module not present', () => {});
});
