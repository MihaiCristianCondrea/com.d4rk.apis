/**
 * @file DOM custom event channel names for GitHub tools route orchestration.
 */

const GitHubToolsEvent = Object.freeze({
  REPO_MAPPER_SUBMIT: 'repo-mapper:submit',
  RELEASE_STATS_SUBMIT: 'release-stats:submit',
  GIT_PATCH_SUBMIT: 'git-patch:submit',
});

export { GitHubToolsEvent };
