/**
 * Legacy resume builder test suite placeholder.
 * The original implementation no longer exists in the current codebase,
 * so this suite is skipped until a new module is available.
 */
describe.skip('resume builder (legacy)', () => {
  test('skipped: resume module has been removed', () => {});
});
