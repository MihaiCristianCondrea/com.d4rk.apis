/**
 * Legacy songs feature scripts no longer ship with the application bundle.
 * Tests are skipped as a placeholder until the feature is reintroduced.
 */
describe.skip('songs feature (legacy)', () => {
  test('skipped: songs module not present', () => {});
});
